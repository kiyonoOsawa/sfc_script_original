<%= form_with(model: friend_user) do |form| %>
  <% if friend_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(friend_user.errors.count, "error") %> prohibited this friend_user from being saved:</h2>

      <ul>
        <% friend_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :friend_id, style: "display: block" %>
    <%= form.number_field :friend_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
