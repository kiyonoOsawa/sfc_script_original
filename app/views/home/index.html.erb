<p id="notice"><%= notice %></p>

<h1>自分の日記</h1>
<%# <%= link_to '友達追加', friends_path, class: 'btn btn-primary' %> 

<table>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>時間</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
    <div class="one_day">
      <tr>
        <td><%= task.taskName %></td>
        <td><%= task.start_time.strftime("%Y-%m-%d %H:%M") %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task.id) %></td>
        <td><%= link_to 'Destroy',task_path(task.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    </div>
    <% end %>
  </tbody>
</table>

<br>

<%# <%= button_to '更新', refresh_date_path, method: :get, class: 'btn btn-primary' %> 

<%= link_to '日記をかく', new_task_path, class: "btn btn-primary" %>

<%# <%= month_calendar events: @tasks do |date, tasks| %>
  <%# <%= date.day %> 

  <%# <% tasks.each do |task| %> 
    <%# <div class="task_sheal"> %>
      <%# <%= link_to task.taskName, task %> 
    <%# </div> %>
  <%# <% end %> 
<%# <% end %> 

<%= month_calendar events: @tasks_by_date do |date, tasks| %>
  <%= date.day %>

  <% tasks.each do |task| %>
    <div class="task_sheal">
      <%= link_to task.taskName, task %>
    </div>
  <% end %>
<% end %>